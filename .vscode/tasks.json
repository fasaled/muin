{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "muin: build vscode extension",
      "type": "shell",
      "command": "bun run --filter muin build",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
