# Muin (CLI)

Install: `npm install -g @fasaled/muin`

```bash
muin document.pdf
muin --mcp document.pdf
```

See the repository README for commands, limits, and the VS Code extension (a separate install).
