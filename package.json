{
  "name": "muin-monorepo",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter @fasaled/muin build",
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "start": "bun packages/cli/src/cli.ts"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
